person={
    name:"Mohamed",
    age:"20",
    Country:"Canada"
}
function logData(){
    let description=(person.name+" is "+person.age+" and lives in "+ person.Country)
    console.log(description)
}
logData()
Math.random(0,3).floor()